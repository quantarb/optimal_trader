"""Signal utilities.

In the refactor, signals are represented as prediction columns on a panel
(e.g., pred_cls__rf, pred_rank__ag). The legacy 'signal producer' pipeline
is intentionally minimized.
"""
