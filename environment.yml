name: optimal_trader
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pandas
  - django
  - jupyterlab
  - ipykernel
  - pytest
  - pip:
      - streamlit>=1.32
      - alpaca-py
      - robin-stocks
      - ib-insync
      - -r requirements-quant-warehouse.txt
      - -r requirements-tradingagents.txt
